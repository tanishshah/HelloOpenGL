#version 330
layout (location = 0) in vec3 loc;
layout (location =1 ) in vec2 texture_pos;

out vec2 texture_loc;

void main(){
	gl_Position = vec4(loc.x, loc.y, loc.z, 1.0);
	texture_loc = texture_pos;
}